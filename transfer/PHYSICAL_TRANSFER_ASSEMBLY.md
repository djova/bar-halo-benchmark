# Conditional full-population assembly: preparation only

22 September 2026. This document prepares the next decision; it does not launch
additional physical trajectories. First require the entire independent cadence
confirmation to terminate, pass all unchanged numerical and unforced gates, and
fit the resource budget. A failed or stopped confirmation permits no physical
qualification. No outcome-based selection of chunks or new sample extension.

If those prerequisites pass and measured CPU permits, retain the original cap of
524,288 stable seed8302 IDs, exactly0through524287. Every primary trajectory must
use the same fourth-order dtmax.01/refinement4 candidate, library and frozen caseB
forecast. Do not mix in the old second-order full sample. That separate historical
attempt stays available with its original failed qualification.

Expected coverage at candidate settings, subject to exact validation:

| IDs, inclusive | N | Source |
| --- | ---: | --- |
| 0–16383 | 16384 | Completed original final-matrix candidate pair |
| 16384–65535 | 49152 | New matched pair, only after a resource decision |
| 65536–196607 | 131072 | Four candidate pairs in the fixed independent confirmation |
| 196608–524287 | 327680 | Five new65536-ID matched pairs, only after a resource decision |

Those counts sum to524288 without gaps or duplicates. Preserve each raw result,
manifest, stable IDs, action–angle initial state, exact forecast/configuration and
source/library hash. Check every array is finite; verify paired identity before
concatenating. The already complete16384prefix is reused only if all settings,
source and library identities actually agree. No resampling, trimming, selective
replacement or mixing of operators. The completed confirmation's new unforced
controls and new step/cadence checks remain separate from physical means.

The primary estimand and decision are unchanged from FORECAST_PROTOCOL.md:
X is the 3D noisy-minus-smooth bar impulse, Y is its matched reduced contrast.
Estimate mean(X-Y), raw mean(X), mean(Y), their covariance and iid Student-t95%
intervals. The fixed coefficient of Y remains1. Report the variance-reduced
forecast-plus-discrepancy estimate separately from the raw3D mean.

Use the frozen forecast's numerical envelope plus the sum of the maximum absolute
endpoints of the qualified step/cadence discrepancy intervals. Enlarge the primary
physical sampling interval by that quantity, as in the original analysis. Compare
with the unchanged +/-1.8126730139721574e-6 operational adequacy band. Whole interval
inside: qualified within this target; wholly outside: resolved failure of this
fixed reduction; boundary overlap: unresolved. These numerical intervals are not
rigorous error bounds or a simultaneous coverage guarantee. No strong dark-matter,
observational or full-halo inference follows from this imposed operator.

The numerical confirmation uses131072of the primary IDs. State explicitly that
the physical inference is conditional on its numerical screen. Additionally report
a fixed DISJOINT diagnostic using IDs0–65535and196608–524287 (393216particles),
excluding all confirmation IDs. Give its complete interval and the same numerical
envelope; no outcome-dependent switch of the primary estimator, selective claim,
or pooling of inconsistent operators. The diagnostic is not a new sample-extension
rule. Contrast its inference with the primary result if they differ.

Before launch, estimate remaining cost from completed confirmation runtimes and
actual prefix-generation overhead, not only the earlier16384timing. Include all
reported scientific CPU, stopped work and prior interruption upper bounds against
the48core-hour ceiling. Record a frozen finite queue and aggregate CPU allowance.
Stop new production by23September18:46:24UTC; retain the final eight-hour review
reserve. Do not launch an infeasible queue merely because the numerical gate passed.
