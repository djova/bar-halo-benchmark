const fs=require('node:fs'),assert=require('node:assert/strict'),{createHash}=require('node:crypto');
const {chromium,webkit}=require('playwright');
const [base,out]=process.argv.slice(2);const cases=[];
(async()=>{for(const engine of [chromium,webkit]){const browser=await engine.launch(engine===chromium?{executablePath:'/usr/bin/google-chrome',args:['--no-sandbox']}:{headless:true});try{for(const width of [1440,390,320]){const page=await browser.newPage({viewport:{width,height:1000},javaScriptEnabled:false});await page.goto(base+'paper.html');const a=page.getByRole('link',{name:/Download PDF/});assert.equal(await a.count(),1);assert(await a.isVisible());assert.equal(await page.evaluate(()=>document.documentElement.scrollWidth>innerWidth+1),false);await a.focus();assert.equal(await a.evaluate(el=>el===document.activeElement),true);const href=await a.getAttribute('href');const pdf=await page.request.get(new URL(href,base).href);assert.equal(pdf.status(),200);assert.match(pdf.headers()['content-type'],/application\/pdf/);const bytes=await pdf.body();assert.equal(bytes.subarray(0,5).toString(),'%PDF-');const manifest=await(await page.request.get(new URL('manifest.json',new URL(href,base)).href)).json();assert.equal(createHash('sha256').update(bytes).digest('hex'),manifest.artifacts.find(a=>a.path.endsWith('.pdf')).sha256);if(width===390||width===1440)await page.screenshot({path:`/tmp/galaxy-mnras-${engine.name()}-${width}.png`});cases.push({engine:engine.name(),width,no_javascript_download:true,no_overflow:true,keyboard_focus:true,content_type:'application/pdf',pdf_hash_matches:true});await page.close();}}finally{await browser.close();}}fs.writeFileSync(out,JSON.stringify({cases,scope:'PDF button, anonymous payload and layout; not physical validation'},null,2)+'\n');console.log('Six desktop/mobile/no-JavaScript PDF download checks passed.');})().catch(e=>{console.error(e);process.exit(1)});
